{
  "name": "opencode-image",
  "version": "0.1.0",
  "description": "Unofficial Docker image for opencode web UI — pull, run, start coding at localhost:7777. Works with plain Docker or MicroSandbox microVMs.",
  "type": "module",
  "scripts": {
    "check": "npm exec -- tsgo --noEmit",
    "sandbox": "tsx example/microsandbox-run.ts"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "microsandbox": "^0.6.4"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "@typescript/native-preview": "^7.0.0-dev.20260527.2",
    "tsx": "^4.22.3"
  }
}
